# PRD — 팀 파티 (가칭: Party Up / 원정대)

> 팀원 5명의 생년월일(+선택 MBTI)로 팀을 "RPG 원정대"로 진단하는 웹 서비스.
> 사주 명리(오행·간지)를 게임 언어(클래스·스탯·시너지 버프)로 번역해 비즈니스/협업 관점의 팀 케미를 보여준다.

## 1. 배경 & 목표

- 기존 서비스(도령 등)는 "무속·귀인·점집" 감성의 1:N(나 중심) 구조.
- 본 서비스는 **N:N 팀 관계**를 다루며, 컨셉을 **RPG 파티 편성**으로 완전히 차별화한다.
- 타깃: 스타트업·프로젝트 팀·스터디 그룹의 팀빌딩/아이스브레이킹.
- 포지셔닝: **재미·팀빌딩 도구**. 채용·인사평가 용도가 아님을 명시(면책 문구).

### 성공 기준 (MVP)
- 파티 생성 → 초대 링크 공유 → 각자 생일 입력 → 파티 대시보드 열람까지 전 흐름이 동작.
- 5명 기준 10쌍 페어 시너지 + 파티 종합 진단 제공.

## 2. 핵심 컨셉 매핑

| 사주 개념 | 게임 번역 |
|---|---|
| 팀 | 원정대(파티) |
| 팀원 | 캐릭터 |
| 일간(日干) 10종 | 클래스 10종 (선봉 기사, 바람 레인저, 태양 마법사, 등불 사제, 산맥 수호자, 대지 연금술사, 강철 버서커, 은빛 검객, 심해 현자, 이슬 점성술사) |
| 오행 분포 | 스탯 5종 — 추진(목) · 열정(화) · 안정(토) · 결단(금) · 통찰(수) |
| 상생(相生) | 서포트 버프 (방향성 있음: A가 B를 강화) |
| 상극(相剋) | 긴장 관계 — "단련 디버프"로 표현하되 성장 서사로 해석 |
| 비화(같은 오행) | 쌍둥이 시너지 |
| 천간합(甲己 등 5종) | 운명의 콤보 (최대 버프) |
| 팀 오행 결핍 | 빈 포지션 (모집 공고 형식으로 표현) |
| MBTI | 전투 스타일 (보조 렌즈, 시너지 점수 보정 ±) |

## 3. 사용자 흐름

1. **파티 생성**: 리더가 파티명 입력 + 자기 캐릭터 등록(이름, 생년월일, 양/음력, MBTI 선택).
2. **모집**: 초대 링크 발급 → 팀원에게 공유.
3. **합류**: 팀원이 링크에서 **각자 자기 생일을 입력** (개인정보 자기 입력 원칙).
4. **진단**: 파티 대시보드에서 캐릭터 시트, 파티 밸런스, 페어 시너지(10쌍), 추천 조합 열람.
5. **공유**: 생일이 노출되지 않는 공개 공유 카드 발급(선택).

## 4. 기능 요구사항

### 4.1 캐릭터 시트 (개인)
- 클래스(일간 기반) + 클래스 설명(협업 성향 중심 서술)
- 스탯 5각형 레이더 (년·월·일주 6글자의 오행 분포)
- 특성: 천간합 보유 여부, 강한/약한 오행
- MBTI 입력 시 "전투 스타일" 뱃지

### 4.2 페어 시너지 (10쌍)
- 시너지 등급/점수 (0~100)
- 관계 유형: 운명의 콤보 / 서포트 버프(방향 표기) / 쌍둥이 시너지 / 단련 관계 / 중립
- 협업 팁 + 주의점 텍스트 (오행 조합별 템플릿)
- MBTI 보정: 두 사람 모두 입력한 경우 ±점수 및 한 줄 코멘트

### 4.3 파티 진단 (팀)
- 파티 오행 밸런스 차트 + 파티 유형 한 줄 (예: "돌격형 원정대 — 추진은 넘치는데 마무리(결단)가 빈다")
- 빈 포지션: 부족 오행 → "이런 동료를 영입하세요" 모집 공고 카드
- 시너지 네트워크 그래프: 5노드 원형 배치, 엣지 색으로 버프/긴장 표시
- 추천 조합(포메이션): 시너지 상위 페어 기반 역할 배치 제안

### 4.4 비기능
- 계산은 전부 서버 순수 함수(외부 API 없음). `lunar-javascript`로 만세력/음양력 처리.
- 시주(출생 시간) 미사용 — 정밀도보다 접근성 우선.
- 개인정보: 생년월일은 본인이 직접 입력, 파티 삭제 시 완전 삭제, 공유 카드에는 생일 미노출. 면책 문구 상시 노출.

## 5. 계산 규칙

### 5.1 사주 산출
- 입력: 생년월일(YYYY-MM-DD), 양력/음력 여부.
- 음력 → 양력 변환 후 년주·월주·일주 산출 (월주는 절기 기준, 라이브러리 처리).
- 오행 카운트: 3주 × (천간+지지) = 6글자. 지지는 본기(주된 오행)로 계산.

### 5.2 시너지 점수 (페어, 방향 있음: A 기준 B)
| 조건 | 점수 |
|---|---|
| 기본 | 60 |
| 천간합 (일간 기준) | +20, 유형 = 운명의 콤보 |
| B가 A를 생(生) | +15, 유형 = 서포트 버프 (B → A) |
| A가 B를 생(生) | +12, 유형 = 서포트 버프 (A → B) |
| 같은 오행 | +10, 유형 = 쌍둥이 시너지 |
| 상극 (어느 방향이든) | -8, 유형 = 단련 관계 |
| MBTI 보정 (둘 다 입력 시) | E/I 조합 +3, N/S 일치 +4, T/F 혼합 +3, J/P 일치 +4 (최대 +14) |
- 최종 0~100 클램프. 표시 등급: 90+ SSS / 80+ S / 70+ A / 60+ B / 그 외 C.
- 페어 점수는 양방향 평균으로 표시하되, 상세에서 방향별 서술 제공.

### 5.3 파티 밸런스
- 5명 오행 합산 → 최다/최소 오행 → 파티 유형 텍스트 (최다×최소 조합 매트릭스).
- 오행 카운트 0인 원소 = 빈 포지션.

## 6. API (MVP 구현 범위)

| Method | Endpoint | 설명 |
|---|---|---|
| POST | `/api/parties` | 파티 생성 (파티명 + 리더 멤버 정보) |
| GET | `/api/parties/{partyId}` | 파티 전체 조회 (멤버 + 분석 포함) |
| DELETE | `/api/parties/{partyId}` | 파티 삭제 |
| POST | `/api/parties/{partyId}/members` | 멤버 등록 (최대 8명) |
| DELETE | `/api/parties/{partyId}/members/{memberId}` | 멤버 삭제 |
| GET | `/api/invites/{token}` | 초대 정보 조회 |
| POST | `/api/invites/{token}/join` | 초대 합류 (멤버 등록과 동일 바디) |

- 분석(analysis/synergies/formation)은 파티 조회 응답에 포함(계산이 결정적이라 조회 시 계산해도 무비용 수준).
- 초대 토큰은 파티 생성 시 자동 발급, 파티 ID와 별개 난수.
- 인증 없음(MVP): 파티 URL 소지 = 열람 권한. 공유 카드 분리는 post-MVP.

### Post-MVP
- `POST /api/parties/{id}/report` — LLM 종합 리포트 (프리미엄)
- `POST /api/parties/{id}/share` + `GET /share/{slug}` — 생일 미노출 공개 카드
- 카카오 로그인, 보관함, 결제

## 7. 데이터 모델

```
Party { id, name, inviteToken, createdAt, members: Member[] }
Member { id, name, birthDate, calendarType: 'solar'|'lunar', mbti?, createdAt }
// 파생(저장 안 함): pillars, class, stats, synergies, partyAnalysis
```

- 저장소: MVP는 로컬 JSON 파일 저장소 (`data/parties.json`), 인터페이스 분리로 추후 DB 교체 가능.

## 8. 기술 스택

- Next.js (App Router) + TypeScript, 플레인 CSS (게임풍 커스텀 디자인)
- `lunar-javascript`: 만세력(절기 기준 월주)·음양력 변환·간지 계산
- 배포 목표: Vercel (post-MVP에서 저장소를 DB로 교체 후)

## 9. 리스크 & 대응

| 리스크 | 대응 |
|---|---|
| 인사평가 오용 인식 | 전 페이지 면책 문구, "팀빌딩·재미" 포지셔닝 고정 |
| 타인 생일 무단 입력 | 초대 링크 자기 입력 흐름을 기본으로 설계 |
| 음력 하루 오차(한·중 음력 차이) | MVP 허용 오차로 명시, 추후 KASI API 검토 |
| 해석 텍스트 품질 | 오행 5×5 템플릿 → 사용자 반응 보고 십성 확장 |
